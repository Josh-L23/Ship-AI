GEMINI_API_KEY=your-key-here
LLM_PROVIDER=mock
LLM_MODEL=gemini-2.0-flash

OPENAI_API_KEY=
GROQ_API_KEY=
OPENROUTER_API_KEY=
XAI_API_KEY=

AGENT_INTAKE_PROVIDER=
AGENT_MARKET_PROVIDER=
AGENT_VISUAL_PROVIDER=
AGENT_PRODUCTION_PROVIDER=
AGENT_MANAGER_PROVIDER=

AGENT_INTAKE_MODEL=
AGENT_MARKET_MODEL=
AGENT_VISUAL_MODEL=
AGENT_PRODUCTION_MODEL=
AGENT_MANAGER_MODEL=

# --- Database ---
# Local development (SQLite, tables auto-created on startup):
DATABASE_URL=sqlite+aiosqlite:///./storage/ship_ai.db

# Supabase (same project as Cursor MCP in .cursor/mcp.json, ref ccmqnviwsbppmlgqgass):
# SUPABASE_URL=https://ccmqnviwsbppmlgqgass.supabase.co
# Get DATABASE_URL from Dashboard → Project Settings → Database → Connection string (URI).
# Prefer the pooler (port 6543, transaction mode) for the FastAPI server.
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
#
# If the schema was already applied (e.g. via Supabase MCP or SQL), sync Alembic without re-running DDL:
#   cd backend && alembic stamp f33360beec80
# If you have an empty Postgres and only use Alembic from this repo:
#   cd backend && alembic upgrade head
